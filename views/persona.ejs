<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Person Name: <%= ID %></h1>
    <h2>Mensaje: <%= MSG %></h2>
    <ul>
      <% switch (MSG) { case 'Bienvenido': %> <% for (let i = 1; i <= TS; i++) {
      %>
      <li><%= MSG %> <%= ID %></li>
      <% } %> <% break; case 'Hola': %> <% for (let i = 1; i <= TS; i++) { %>
      <li><%= MSG %> <%= ID %></li>
      <% } %> <% break; case 'Adios': %> <% for (let i = 1; i <= TS; i++) { %>
      <li><%= MSG %> <%= ID %></li>
      <% } %> <% break; default: break; } %>
    </ul>
  </body>
</html>
